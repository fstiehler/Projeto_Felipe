
if (typeof window !== "undefined") {
  // Declare window.FB if it is used somewhere in the project
  window.FB = window.FB || {};
}

// styled-components doesn't need TypeScript types in JS, so we can skip that
